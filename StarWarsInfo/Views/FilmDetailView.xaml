<UserControl x:Class="StarWarsInfo.Views.FilmDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StarWarsInfo.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <DataGrid ItemsSource="{Binding FeaturedCharecters}" Grid.Column="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Home World" Binding="{Binding HomeWorld}"/>
            </DataGrid.Columns>
        </DataGrid>
        <ListBox ItemsSource="{Binding FeaturedPlanets}" Grid.Column="1"/>
        <TextBlock Text="{Binding OpeningCrawl}" Grid.Column="2"/>
    </Grid>
</UserControl>
